import { FC } from 'react';
import styled from '@emotion/styled';

const Wrapper = styled.div``;

interface InvestorsProps {}

export const Investors: FC<InvestorsProps> = ({}) => {
  return <Wrapper>Investors</Wrapper>;
};
